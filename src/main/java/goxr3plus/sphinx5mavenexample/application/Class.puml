@startuml

package main.java.goxr3plus.sphinx5mavenexample.application {
    class MainGame {
        - TextToSpeech tts
        - LetterRecognition letterRecognition
        - JLabel wordLabel
        - JTextField inputField
        - JButton startButton
        - JButton answerButton
        - JButton replayButton
        - String currentWord
        - List<String> wordList
        + MainGame()
        + void actionPerformed(ActionEvent e)
        + void startGame()
        + void onLetterRecognized(String recognizedLetter)
    }
    class MainGame.MessageScreen {
        - JLabel messageLabel
        - JButton quitButton
        + MessageScreen(String message)
    }

    class SpellingBeeUI {
        - JButton startButton
        - JLabel titleLabel
        - JLabel subtitleLabel
        - TextToSpeech tts
        + SpellingBeeUI()
        + void startGame()
    }

    class TextToSpeech {
        - AudioPlayer tts
        - MaryInterface marytts
        + TextToSpeech()
        + void speak(String text, float gainValue, boolean daemon, boolean join)
        + void stopSpeaking()
        + Collection<Voice> getAvailableVoices()
        + MaryInterface getMarytts()
        + List<AudioEffect> getAudioEffects()
        + void setVoice(String voice)
    }

    class AudioPlayer extends Thread {
        - AudioInputStream ais
        - LineListener lineListener
        - SourceDataLine line
        - int outputMode
        - Status status
        - boolean exitRequested
        - float gain
        + Status
        + void setAudio(AudioInputStream audio)
        + void cancel()
        + SourceDataLine getLine()
        + float getGainValue()
        + void setGain(float fGain)
        + void run()
    }

    class LetterRecognition {
        - LetterRecognitionCallback callback
        - LiveSpeechRecognizer recognizer
        - Logger logger
        - boolean ignoreSpeechRecognitionResults
        - boolean speechRecognizerThreadRunning
        - ExecutorService eventsExecutorService
        - List<String> recognizedLetters
        + LetterRecognition()
        + void setLetterRecognitionCallback(LetterRecognitionCallback callback)
        + void startSpeechRecognition(int delayMilliseconds)
        + void stopSpeechRecognition()
        + void ignoreSpeechRecognitionResults()
        + void stopIgnoreSpeechRecognitionResults()
        + void stopRecognition()
    }
    interface LetterRecognitionCallback {
        void onLetterRecognized(String recognizedLetter)
    }
}

MainGame --> TextToSpeech
MainGame --> LetterRecognition
MainGame --> JLabel
MainGame --> JTextField
MainGame --> JButton
MainGame --> List
MainGame --> MainGame.MessageScreen

MainGame.MessageScreen --> JLabel
MainGame.MessageScreen --> JButton

SpellingBeeUI --> JButton
SpellingBeeUI --> JLabel
SpellingBeeUI --> TextToSpeech

TextToSpeech --> AudioPlayer
TextToSpeech --> MaryInterface
TextToSpeech --> Voice
TextToSpeech --> AudioEffect
AudioPlayer --> AudioInputStream
AudioPlayer --> LineListener
AudioPlayer --> SourceDataLine

LetterRecognition --> LetterRecognitionCallback
LetterRecognition --> LiveSpeechRecognizer
LetterRecognition --> Logger
LetterRecognition --> ExecutorService
LetterRecognition --> List

@enduml
